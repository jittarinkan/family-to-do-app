<h1>Tasks assigned to <%= @family_member.name %></h1>

<%= link_to "Add Task", new_family_member_task_path %>

<table class="table">
  <thead>
    <tr>
      <th scope="col">Task</th>
      <th scope="col">Status</th>
      <th scope="col">Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
    <tr>
      <td> <%= task.description %> </td>
      <td> <%= task.status %></td>
      <td>
        <%=
          button_to 'Mark as complete',
          { action: 'update', id: task.id },
          method: :put,
          params: { task: { status: :complete } }
        %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Main Dashboard', dashboard_index_path %>

<%= paginate @tasks %>
